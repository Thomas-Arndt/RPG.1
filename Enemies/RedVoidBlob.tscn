[gd_scene load_steps=10 format=2]

[ext_resource path="res://Enemies/RedVoidBlob-idle-move.png" type="Texture" id=1]
[ext_resource path="res://Enemies/RedVoidBlob-Faded-idle-move.png" type="Texture" id=2]
[ext_resource path="res://Enemies/VoidBlob.tscn" type="PackedScene" id=3]
[ext_resource path="res://Enemies/RedVoidBlob.gd" type="Script" id=4]

[sub_resource type="Shader" id=2]
code = "shader_type canvas_item;

uniform bool active = false;

	void fragment() {
		vec4 previous_color = texture(TEXTURE, UV);
		vec4 white_color = vec4(1.0, 1.0, 1.0, previous_color.a);
		vec4 new_color = previous_color;
		if (active){
			new_color = white_color;
		}
		COLOR = new_color;
	}"

[sub_resource type="ShaderMaterial" id=3]
resource_local_to_scene = true
shader = SubResource( 2 )
shader_param/active = false

[sub_resource type="Shader" id=4]
code = "shader_type canvas_item;

uniform bool active = false;

	void fragment() {
		vec4 previous_color = texture(TEXTURE, UV);
		vec4 white_color = vec4(1.0, 1.0, 1.0, previous_color.a);
		vec4 new_color = previous_color;
		if (active){
			new_color = white_color;
		}
		COLOR = new_color;
	}"

[sub_resource type="ShaderMaterial" id=5]
resource_local_to_scene = true
shader = SubResource( 4 )
shader_param/active = false

[sub_resource type="AnimationNodeStateMachinePlayback" id=1]

[node name="RedVoidBlob" instance=ExtResource( 3 )]
script = ExtResource( 4 )

[node name="FullSprite" parent="." index="1"]
material = SubResource( 3 )
texture = ExtResource( 1 )

[node name="HalfSprite" parent="." index="2"]
material = SubResource( 5 )
texture = ExtResource( 2 )

[node name="AnimationTree" parent="." index="4"]
parameters/playback = SubResource( 1 )

[editable path="HurtBox"]
[editable path="PlayerDetectionZone"]
[editable path="HitBox"]
[editable path="SoftCollision"]
